#include "lists.h"

/**
 * delete_dnodeint_at_index - function that deletes the
 * node at index of a linked list
 * @head: thea head of the linked list
 * @index: the index of the number to be removed
 * Return: 1 if success -1 if fail
 */

int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
{
	dlistint_t *temp;
	unsigned int i;

	if ()
}
